h1 = t('activerecord.models.skirt_type')
hr
= search_form_for @q do |f|
  .row
    .col-sm-5
      = f.input :name_cont, required: false, label: t('activerecord.attributes.skirt_type.name')
      = hidden_field_tag :mode, params[:mode]
  .btn-toolbar
    .btn-group.mr-2
      = f.submit class: "btn btn-primary"
    .btn-group.mr-2
      = button_tag t('helpers.submit.close'), type: 'button', class: "btn btn-secondary", onclick: 'window.close()'
p
table.table.table-striped.table-hover.table-responsive.table-bordered
  thead
    tr
      th = sort_link(@q, :name)
  tbody
    - @skirt_types.each do |skirt_type|
      tr
      - if params[:mode] = 'dialog'
        td onclick="window.opener.dialog_select('skirt_type', '#{skirt_type.name}', 'skirt_type_id', '#{skirt_type.id}');window.close();"
          = skirt_type.name
      - else
        td
          = skirt_type.name

br
= paginate @skirt_types
